# Public Sentry DSN for the Leonardo docs UI (browser SDK).
# Copy to .env.local (gitignored) and fill in your project DSN.
VITE_SENTRY_DSN=

# Optional: set at build time so events are tagged with a release
# VITE_APP_VERSION=leonardo-ui@1.4.5
